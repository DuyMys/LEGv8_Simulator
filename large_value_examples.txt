// Option 1: Use SUBI for small negative values
SUBI X1, XZR, #2     // X1 = 0 - 2 = -2
MOV X2, #3
SMULH X3, X1, X2

// Option 2: Build large values with multiple MOVZ/MOVK instructions
// For 0xFFFFFFFFFFFFFFFE, you would need:
// MOVZ X1, #0xFFFE, LSL #0    // Set bits 0-15
// MOVK X1, #0xFFFF, LSL #16   // Set bits 16-31  
// MOVK X1, #0xFFFF, LSL #32   // Set bits 32-47
// MOVK X1, #0xFFFF, LSL #48   // Set bits 48-63
